#!/usr/bin/env bash
CMD="${2:-}"
case "$CMD" in
  *QCFG*) echo "OK" ;;
  *EGMR*)
    echo "OK"
    echo "$CMD" >> /tmp/norypt_mock_at_calls
    ;;
  *CGSN*)
    cat /tmp/norypt_mock_imei 2>/dev/null || echo "356741082934756"
    ;;
  *) echo "ERROR" ;;
esac
